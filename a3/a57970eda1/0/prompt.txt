git status

---

So, First of all read the GEMINI.md file which is in ~ directory, After that lets build a project using sarvaam.ai API Key, Where The main scope is converting the text file (Any language in the file the content.) need to write a python script where in that it read the story.txt file and read the content and generates a audio file using the api key which i'll paste the key later at the end, so now this is the project, Go with the best approach python script, when i run the file it gives the audio.mp3 file so, this the scope. For this project what ever dependencies are required and files are required create them. So, First Create a README.md file and then rest of the things. I Hope You will Crack It, Your a Senior Developer. So, Go.

---

pavankumar19@Pa1-Kumr-P:~/text-to-audio$ pip3 install -r requirements.txt
error: externally-managed-environment

× This environment is externally managed
╰─> To install Python packages system-wide, try apt install
    python3-xyz, where xyz is the package you are trying to
    install.

    If you wish to install a non-Debian-packaged Python package,
    create a virtual environment using python3 -m venv path/to/venv.
    Then use path/to/venv/bin/python and path/to/venv/bin/pip. Make
    sure you have python3-full installed.

    If you wish to install a non-Debian packaged Python application,
    it may be easiest to use pipx install xyz, which will manage a
    virtual environment for you. Make sure you have pipx installed.

    See /usr/share/doc/python3.12/README.venv for more information.
 I am getting this thing when i run requirements.txt. So,When you fix this what will be the issue.

---

So, i need an API right, how to get it from sarvaam.ai guide me step by step.

---

So, till now update the readme.md file and git add them and commit them individually and push them. And one thing when coming to output.mp3 when it is pushed to github when i click on the output.mp3 it should get played in the github, so these were the changes.